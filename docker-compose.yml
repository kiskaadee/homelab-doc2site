services:
  docs:
    build: .
    container_name: ${SERVICE_NAME:-docs}
    restart: unless-stopped
    volumes:
      - ${PROJECT_PATH:-./data}:/docs:ro
      - ./app:/app:ro
    environment:
      - DOCS_ROOT=/docs
      - PORT=8000
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${SERVICE_NAME:-docs}.rule=Host(`${SERVICE_DOMAIN:-docs.roadtotech.me}`)"
      - "traefik.http.routers.${SERVICE_NAME:-docs}.entrypoints=websecure"
      - "traefik.http.routers.${SERVICE_NAME:-docs}.tls=true"
      - "traefik.http.routers.${SERVICE_NAME:-docs}.service=${SERVICE_NAME:-docs}-svc"
      
      # HTTP to HTTPS redirect
      - "traefik.http.routers.${SERVICE_NAME:-docs}-red.rule=Host(`${SERVICE_DOMAIN:-docs.roadtotech.me}`)"
      - "traefik.http.routers.${SERVICE_NAME:-docs}-red.entrypoints=web"
      - "traefik.http.routers.${SERVICE_NAME:-docs}-red.middlewares=https-redirect@docker"
      
      # Target Port
      - "traefik.http.services.${SERVICE_NAME:-docs}-svc.loadbalancer.server.port=8000"

networks:
  proxy:
    name: ${PROXY_NETWORK}
    external: true
